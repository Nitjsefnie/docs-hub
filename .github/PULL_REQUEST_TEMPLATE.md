<!--
Section order is fixed. Sections marked (required) always appear — if the
answer is "none", say so explicitly rather than deleting the section.
Sections marked (optional) or (conditional) are deleted entirely, header
included, when they do not apply. Do not add sections.
-->

## Summary

<!-- required: 1-2 sentences — what changed and why. -->

## Related Issues and Pull Requests

<!--
conditional: the issue this PR resolves, plus related work. Closing keywords
belong here and nowhere else in the body. Delete this section entirely if the
PR resolves nothing and relates to nothing.

    Fixes #123
    Relates to #456
    Depends on #789

GitHub only auto-closes on merge into the default branch.
-->

## Changes

<!-- required: bullet list of concrete changes — files, modules, behavior. -->

-

## Testing

<!--
required: what was actually run or checked, with the real command and its
real output. "Tests pass" without the run is not evidence. If nothing was
tested, say so and why. For anything touching auth, versioning, or delete,
state what you tried that *should* fail and confirm it did.
-->

```
$ .venv/bin/pytest -v
```

## Bugs Discovered

<!--
conditional: bugs this work uncovered, each already filed as its own issue.
Issue number plus the issue's literal title, copied exactly as filed — this
is a pointer list, not a place to describe findings. Delete this section
entirely if none were found.

    - #123 <issue title, verbatim>
-->

## Breaking Changes

<!-- optional: delete entirely if there are none. -->

## Follow-ups / Known Limitations

<!-- optional: delete entirely if there are none. -->

## Dependencies

<!-- optional: delete entirely if there are none. -->

## Footer

<!--
required, last line of the report. Every agent/model that touched this PR,
with the role(s) it played — one entry per model, roles merged:

    Generated by <model> (<role>, <role>, ...), <model> (<role>, ...)

Leave it off only if no model touched the PR. This and the commit
Co-Authored-By trailer are the only disclosure surfaces — do not add a
disclosure line anywhere else.
-->
