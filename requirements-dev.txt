# Lint / type toolchain, mirroring the house set. Versions pinned so a tool
# release cannot turn CI red on an unchanged commit; Dependabot bumps them.
# The runtime dependencies in requirements.txt are installed alongside these
# in CI, because both pylint and pyright resolve `import fastapi` for real.
pycodestyle==2.14.0
pylint==4.0.8
pyright==1.1.411
